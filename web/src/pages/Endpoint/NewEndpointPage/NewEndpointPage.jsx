import NewEndpoint from 'src/components/Endpoint/NewEndpoint'

const NewEndpointPage = ({ userId, organizationId }) => {
  return <NewEndpoint userId={userId} organizationId={organizationId} />
}

export default NewEndpointPage
