import EditWebhookEventLogCell from 'src/components/WebhookEventLog/EditWebhookEventLogCell'

const EditWebhookEventLogPage = ({ id }) => {
  return <EditWebhookEventLogCell id={id} />
}

export default EditWebhookEventLogPage
